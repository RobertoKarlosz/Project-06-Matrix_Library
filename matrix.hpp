#include <iostream>
#include <vector>

template <typename T>
class Matrix {
    size_t rows;
    size-t cols;
    vector<T> values;
public:
    Matrix() {}
    Matrix() {}

    // Getters

    size_t getRows() {}
    size_t getCols() {}

    // Operators for Matrix operations

    Matrix<T> operator-() {}
    Matrix<T> operator-() {}
    Matrix<T> operator+() {}
    Matrix<T> operator*() {}
    Matrix<T> operator*() {}
    Matrix<T> operator/() {}
    Matrix<T> operator/() {}

    void operator+=() {}
    void operator-=() {}
    void operator*=() {}
    void operator/=() {}

    // Special operands

    Matrix<T> transpose() {}
    Matrix<T> multiplyElements() {}
};